import { View, Text } from 'react-native';
import React from 'react';
import OnboardingBlock from '@/components/ui/core/block/onboarding-block';

export default function onboarding() {
  return <OnboardingBlock />;
}
